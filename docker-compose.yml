services:
  mongodb:
    image: mongo:7.0
    container_name: calendario-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: calendario
    volumes:
      - mongo_data:/data/db
    networks:
      - calendario-network

volumes:
  mongo_data:

networks:
  calendario-network:
    driver: bridge
